package com.yourcompany.planetxnative

import com.unity3d.player.UnityPlayerGameActivity

class UnityHolderActivity : UnityPlayerGameActivity() {
    // Unity 6 uses UnityPlayerGameActivity which handles all the lifecycle
    // and player management automatically. Just extend it and you're done!
    // The native library loading is handled by the parent class.
}

